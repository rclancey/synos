import React from 'react';

export const AudiobookList = ({ onClose }) => (
  <div className="underConstruction">
    Sorry, this screen is not yet available.
    <div className="link" onClick={onClose}>Close</div>
  </div>
);

